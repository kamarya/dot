[alias]
	gl = config --global -l
	last = log -1 HEAD --stat
	ll = log --oneline
	st = status -sb
	fa = fetch --all
	c = checkout
	b = branch
	d = diff
	s = status
	amd = commit --amend --reset-author --date="now"

[user]
	name = KAMARY
	email = kamary@internet.online
[core]
	editor = vim
